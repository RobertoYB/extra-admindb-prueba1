services:
  mysql:
    image: mysql:8

    environment:
      MYSQL_ROOT_PASSWORD_FILE: /run/secrets/root_password
      MYSQL_DATABASE: extra-admindb-prueba1
      MYSQL_USER: app_user
      MYSQL_PASSWORD: mi_contrasena

    ports:
      - "3306:3306"

    secrets:
      - root_password

secrets:
  root_password:
    file: ./secrets/root_password.txt
